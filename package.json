{
  "name": "brightsmile",
  "private": true,
  "scripts": {
    "generate": "pnpm --filter api run generate",
    "dev": "turbo run dev",
    "build": "pnpm run generate && pnpm -r run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.9.14"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
