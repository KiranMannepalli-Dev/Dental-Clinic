import { ReactNode } from "react";

export const metadata = {
  title: "Services | Heshvitha Multi Speciality Dental Hospital",
  description: "Explore our comprehensive dental services — cosmetic dentistry, orthodontics, implants, root canals, pediatric care, and emergency treatments.",
};

export default function ServicesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
